rm(list = ls())

#
best.known <- read.table("assets/best_known/best_known.txt")
# best.known <- data.frame(best.known)
# colnames(best.known) <- c("Instance", "BestKnown")

# print(ncol(best.known))

print(best.known)